# Worker Prompt

## Context

Read the roadmap, recent commits, and the current task.

## Objective

Implement unit tests for specific Firebase Firestore helper functions in `src/lib/firestore.ts` related to `nailItems` collection CRUD operations. This directly addresses Phase 2.1 "Test coverage" from the roadmap.

## Allowed Scope

- `src/` (except `src/main.tsx`)
- `src/lib/` helpers (`src/lib/firestore.ts` for modifications, and `src/lib/storage.ts`, `src/lib/auth.ts`, `src/lib/publicShares.ts` for context if needed)
- `src/__tests__/` (new test files)
- `src/App.
